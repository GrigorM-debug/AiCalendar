# yaml-language-server: $schema=https://raw.githubusercontent.com/Azure/azure-dev/main/schemas/v1.0/azure.yaml.json

name: ai-calendar
services:  
  app:
    language: dotnet
    project: ./AiCalendar.AppHost/AiCalendar.AppHost.csproj
    host: containerapp
